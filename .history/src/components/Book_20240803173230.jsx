import React from 'react'
import { Group } from 'three'

const Book = ({...props}) => {
  return (
               <group props={...props}>
                              {
                                             
                              }
               </group>
  )
}

export default Book